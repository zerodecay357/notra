# Extra dependencies for building the native desktop app (app/desktop.py,
# notra.spec). Not needed to just run Notra via ./run.sh in a browser —
# install this only on a machine building an installer.
-r requirements.txt
PySide6>=6.7
pyinstaller>=6.10
